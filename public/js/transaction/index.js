

const deleteReview = (identifier) => {
    //1. Jalanin get pair review id
    //2. delete review by review id (in reviewModel) and update status
    //3. refresh
}

const editReview = (identifier) => {
    //1. Jalanin get pair review id
    //2. same as add
    //3. Edit instead of Add
}